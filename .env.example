# Database
POSTGRES_DB=hve
POSTGRES_USER=hve
POSTGRES_PASSWORD=change_me_in_production

# JWT
JWT_SECRET=change_me_in_production_min_32_chars
JWT_REFRESH_SECRET=change_me_refresh_in_production_min_32

# App
NODE_ENV=production
FRONTEND_URL=http://localhost:5173
VITE_API_URL=http://localhost:3001

# Email (SMTP)
SMTP_HOST=mailhog
SMTP_PORT=1025
SMTP_USER=
SMTP_PASS=
SMTP_FROM=noreply@haarlemmermeervoorelkaar.nl

# For production (e.g. SendGrid, Postmark, etc.)
# SMTP_HOST=smtp.sendgrid.net
# SMTP_PORT=587
# SMTP_USER=apikey
# SMTP_PASS=your_sendgrid_api_key
